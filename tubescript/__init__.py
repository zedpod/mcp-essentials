"""tubescript — YouTube transcript extraction with format export.

    tubescript.core   — pure logic; uses youtube-transcript-api.
    tubescript.server — FastMCP/stdio server. Run `python -m tubescript`.
    tubescript/owui.py — generated single-file OWUI bundle.
"""

__version__ = "1.0.0"
