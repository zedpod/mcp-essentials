"""sitepulse — DNS, RDAP, SSL, HTTP health snapshot for a domain.

    sitepulse.core   — pure logic.
    sitepulse.server — FastMCP/stdio server. Run `python -m sitepulse`.
    sitepulse/owui.py — generated single-file OWUI bundle.
"""

__version__ = "1.0.0"
